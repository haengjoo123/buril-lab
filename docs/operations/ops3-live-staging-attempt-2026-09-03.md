# Ops3 Staging 실제 검증 — 2026-09-03

1·2차 전체 결과는 **실패**이며 그대로 보존한다. 1차의 API 성공과 3차 사진 전용 검증 성공을 각각 기록한다. 세 검증의 대상은 동일한 runtime SHA `a703570f2e4cc6ad773b051f583ec04c060caaf0`이며, 이후 추가된 음성 자료 조회 제한 코드의 배포 성공으로 확대하지 않는다. 운영 변경은 없다.

후속 2a3 SHA의 배포와 전체 실제 검증은 별도로 성공했다. 현재 상태는 [수정 SHA의 Staging 배포·검증 완료](ops3-staging-verified-2026-09-03.md)를 우선하며 아래 과거 실패를 덮어쓰지 않는다.

## 정확한 대상과 실행

- runtime SHA: `a703570f2e4cc6ad773b051f583ec04c060caaf0`
- Staging Pages: `7237c27b-e24f-4e74-8889-f1aeb1da4f74`
- 원 배포 run: `33694980335` — 성공 및 서명 정리 완료
- 검증 코드 SHA: `2ed38316ca2b0201932213597692f3c2f2e3e0ad` (PR #70)
- 검증 코드 main Quality: `33707303037` — 4/4 성공
- 실제 검증 run: [33707675254](https://github.com/haengjoo123/buril-lab/actions/runs/33707675254)
- 실제 검증 시각: 2026-09-03 02:28:06~02:28:44 UTC
- 새 provider 자격값·lease·배포: 0건. 기존 Staging 실행 환경만 사용.

## 통과한 범위

- custom·immutable Access 보호 및 동일 runtime SHA.
- 신규·레거시 AI 6개와 음성 3개의 미인증 JSON 401.
- JSON 404/405, 금지 Origin 403, 잘못된 UUID/JSON 400, 초과 JSON 413 및 보호 헤더.
- 신규·레거시 라벨 분석·분류·폐기 참고 응답: 모두 실제 API 성공. 분류와 폐기 참고는 `responseSource=ai`를 요구했다.
- 부족한 폐기 입력은 `needs_input` 및 `depositAllowed=false`를 유지.
- 음성 폐기: 고정 redirect 문구와 검토 화면 열기만 수행, 실제 폐기 기록 0건.
- 합성 재고 위치 응답 계약 성공. 이 응답은 공급자 사용 여부를 노출하지 않으므로 독립적인 AI 호출 증거로 세지 않는다.
- 합성 문장 TTS MP3 52,224 bytes, 같은 음원의 STT `gpt-transcribe` 성공.
- 앱 요청 25회, 유료 처리가 가능한 정상 요청 9회. 공급자 내부 호출 총수로 해석하지 않는다.

별도 로컬 실시간 PubChem 연동시험도 2026-09-03 02:27 UTC에 7/7 성공했다. 일반 unit suite의 기존 7개 보류를 해제한 것으로 표시하지 않는다.

## 실패와 재검증

`browser-login-and-cabinet` 단계는 약 5.7초 뒤 `UNEXPECTED_FAILURE_REDACTED`로 종료됐다. 시약장과 사진은 생성되기 전이었다. 원문 오류는 의도적으로 저장하지 않았으므로 이 증거만으로 실패한 개별 동작이나 앱 결함을 확정할 수 없다.

새 독립 검사에는 Playwright assertion 기본값 5초가 적용됐고 기존 Gate0 Staging 기준은 15초다. 기준을 15초로 통일하고 고정된 세부 단계·상태 번호·오류 개수를 추가한다. 이는 확인된 검사 설정 차이이며, 해당 차이가 이번 실패의 유일한 원인이라고 주장하지 않는다. 다음 검사는 유료 호출을 반복하지 않는 `photo` 범위로 실행한다.

## 정리 증거

- `finally`: 새 별칭 4건, nonce로 특정한 AI 캐시 4건 제거. 새 사진·시약장·피드백은 각각 0건.
- 정리 결과 `remainingRunArtifacts=0`; 이후 `always` 단계도 pending journal 없음 확인.
- 02:31 UTC 별도 Supabase 집계: 해당 run 시약장·캐시, 실행 이후 합성 계정 별칭·피드백·사진, 합성 계정 refresh 세션 모두 0건.
- 재사용 합성 계정·연구실·기준 재고·정책·기존 음성 행·감사 이력은 유지했다.
- 이미 발급된 access JWT가 즉시 무효화됐다고 주장하지 않는다.

## 1차 종료 시 열려 있던 항목

- 같은 SHA의 실제 브라우저 WebP 업로드·크기·SHA-256·이미지 표시 검증.
- 음성 별칭용 외부 PubChem/KOSHA 조회의 요청·응답 본문 종료시간과 크기 상한 보완.
- 운영 2의 실제 일일 백업 2회, 24시간·7일 관찰 이후에만 운영 3 배포 가능.
- 운영 4~12와 최종 목표는 미완료. RPO 1시간·guided·파일럿·유료 출시도 닫지 않는다.

## 2차 사진 전용 검증 — 실패 위치 확인

- run `33708949957`, 검증 코드 `258e42125d8afb7028b11694975f6299571224d8` (PR #71), main Quality `33708628776` 4/4 성공.
- 대상 runtime·Pages ID는 1차와 동일하다. `verificationScope=photo`, 앱 API 검사·유료 요청 모두 0회.
- 02:48:22~02:48:36 UTC에 Access·로그인(HTTP 200)·연구실 선택·시약장 목록 열기를 통과했다. CSP·페이지 오류·Access 헤더 외부 전달·예상 밖 유료 요청은 모두 0건.
- 실패 위치는 `browser-create-cabinet`. 생성 전 약 0.9초 안에 종료됐고 새 시약장·사진·캐시·별칭·피드백은 0건, 정리도 완료됐다. 별도 Supabase 집계에서 해당 run 시약장·캐시·실행 이후 사진·합성 계정 refresh 세션 0을 확인했다.
- 코드에서 데스크톱의 상단과 목록 하단에 동일 이름 생성 버튼이 각각 존재함을 확인했다. 기존 전체 페이지 선택자는 두 개를 동시에 선택해 거부된다. `MainLayout`은 숨겨진 모바일 트리도 함께 포함하므로 사진 파일 입력과 이미지도 보이는 `main` 안으로 범위를 좁혀야 한다.
- 실제 Chromium 회귀시험은 두 보이는 생성 버튼과 숨겨진 모바일 파일 입력을 재현해, 기존 선택자의 모호성 오류와 수정된 상단 버튼·파일 입력 선택을 검증한다. 앱 UI 자체나 생성·업로드 권한을 우회하지 않는다.
- 15초 대기시간 통일만으로는 해결되지 않았다. 두 실패 run은 그대로 보존하고 수정 후 사진 검증을 다시 실행한다.

## 3차 사진 전용 검증 — 성공

- run [33710162981](https://github.com/haengjoo123/buril-lab/actions/runs/33710162981), 검증 코드 `44fdeb9f7ffb3668576ce238652f1bf8fe1614b8` (PR #72). PR Quality `33709492324`, main Quality `33709791418` 모두 4/4 성공 후 실행했다.
- 대상 runtime·Pages ID는 1·2차와 동일하다. 검증 전용 실행이므로 새 배포·provider 토큰·lease는 각각 0건이다. `verificationScope=photo`이며 별도 API 검사·유료 요청도 각각 0회다.
- 실제 검증 구간은 03:07:41~03:07:58 UTC다. Access·동일 SHA·실제 로그인(HTTP 200)·합성 연구실 선택·화면 시약장 생성·실제 파일 입력을 통과했다.
- 2,400×1,600 합성 PNG가 업로드 전에 **1,920×1,280 WebP, 20,602 bytes**로 변환됐다. 업로드 본문·저장된 사진·재조회한 사진의 SHA-256과 크기가 같고 화면 이미지도 표시됐다.
- 사진 SHA-256: `b40ca47d9cf6b5dff76c8ad0c8771875cbb544bac89c472d05ab9bf5ff4fdb21`.
- CSP 위반·페이지 오류·Access 자격 헤더 외부 전달·예상 밖 유료 요청은 각각 0건이다. 이 합성 시험 한 건이 실제 모든 이미지 형식·단말의 성공을 보장한다는 뜻은 아니다.
- `finally`에서 이번 사진 1개와 시약장 1개를 정확히 확인해 삭제했다. `remainingRunArtifacts=0`; `always` 단계도 pending journal 없음이다. 기존 자료·감사 이력·재사용 합성 계정은 보존했다.
- 03:14 UTC 별도 Supabase 집계에서 해당 run 시약장·캐시·실행 이후 합성 사진·합성 계정 refresh 세션은 모두 0건이었다. access JWT의 즉시 무효화를 주장하지 않는다.

## 3차 종료 당시 남은 범위와 후속 결과

기존 배포 SHA의 신규·레거시 AI/음성 계약과 실제 WebP 업로드는 위의 서로 다른 검증에서 확인됐다. 3차 종료 시 PR #72의 외부 PubChem/KOSHA 조회 제한은 아직 미배포였다. 이후 보호된 main SHA `2a3ea54b7ded020fdec93fb8cf0772b6b6d3d20b`의 Pages-only Staging run `33712590709`와 같은 SHA의 전체 실제 검증 `33713297440`을 따로 실행해 모두 성공했다. [후속 증거](ops3-staging-verified-2026-09-03.md)에 정확한 배포·정리·시험 결과를 기록한다.

운영 2의 실제 일일 백업은 여전히 0/2이고, 24시간·7일 관찰 기간도 아직 지나지 않았다. 이를 건너뛰어 운영 3을 배포하지 않는다. 운영 4~12, RPO 1시간, guided 음성, 파일럿·유료 출시는 미완료다.
